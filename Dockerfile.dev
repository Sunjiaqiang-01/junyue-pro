# 君悦SPA - 开发环境Dockerfile

FROM node:20-alpine

# 设置工作目录
WORKDIR /app

# 安装基础工具
RUN apk add --no-cache \
    bash \
    git \
    python3 \
    make \
    g++ \
    libc6-compat

# 复制package文件
COPY package*.json ./
COPY prisma ./prisma/

# 安装依赖
RUN npm install

# 生成Prisma Client
RUN npx prisma generate

# 复制所有文件
COPY . .

# 暴露端口
EXPOSE 3000
EXPOSE 5555

# 启动命令（将由docker-compose覆盖）
CMD ["npm", "run", "dev"]

